import fonctions.py
import donnees.py

""" Je pars du principe illusoire que toutes les entrées sont correctes. """


rejouer = true
while rejouer = true:

    input_name()
    choose_word()
    word_pendu = input_char()
    print_word(word_pendu)
    
    
    
    
    
    
    rejouer = input("Voulez vous rejouer ? o/n)
    if rejouer = "o":
        rejouer = true
    else:
        print("A une prochaine, " {} ". Votre score actuel est " {} \
              ". Il va être enregistré pour une prochaine partie.".format(name, score))
        rejouer = false
